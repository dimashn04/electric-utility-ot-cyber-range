# Screenshots

Optional portfolio screenshots can be stored here.

Suggested captures:

- `hmi-normal-closed.png`: HMI baseline with breaker `CLOSED`, RTU connected, and live telemetry.
- `hmi-normal-open.png`: normal HMI/SCADA breaker operation with valid RTU event and no critical detections.
- `hmi-spoofed-attack-detection.png`: breaker `OPEN`, near-zero current/load, alarm banner, and SCADA detections visible after the default attack.
